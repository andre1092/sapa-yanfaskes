---
description: 
---

[START]
   │
   ▼
1. Create Implementation Plan (Draft a detailed plan answering the user's specific prompt)
   │
   ▼
2. Improvement/Revision (Refine the plan based on initial user feedback, if any)
   │
   ▼
3. Approval (Halt execution until the user grants explicit approval, e.g., "SETUJU" or "APPROVED")
   │
   ▼
4. Create Tasks (Generate a minimal, hyper-focused checklist mapping only the approved items)
   │
   ▼
5. Execute Only What Is Commanded (Write code and update files: `struktur.md`, `feature.md`, `source.md`, `memories.md`)
   │
   ▼
6. Task Checklist (Audit the current state and tick off completed tasks)
   │
   ▼
7. Test -> Error -> Fix -> Test (Run local builds. If errors are caught, debug immediately and re-test)
   │
   ▼
8. Success (Verify performance criteria is met, response time < 2s, and code compiles cleanly)
   │
   ▼
9. Deploy to Production (Mandatory: Execute git commit, push to main branch to trigger Vercel deployment, and log to `memories.md`)
   │
   ▼
10. Walkthrough (Deliver a final interactive code demonstration and summary to the user in Indonesian)
   │
   ▼
[END]
